// products or and item class
function item(sku, name, description, price, image, review) {
    this.sku = sku; // product number (SKU stands for stock keeping unit)
    this.name = name;
    this.description = description;
    this.price = price;
    this.image = image;
    this.review = review;
}
